---
description: 
globs: 
alwaysApply: false
---
 Frontend Integration
CORS (Cross-Origin Resource Sharing): 后端Flask必须配置CORS来允许前端的跨域请求。
Python

# 在 app/__init__.py 中
from flask_cors import CORS

# ... 创建 app 实例后 ...
# 为所有 /api/ 开头的路径启用CORS
CORS(app, resources={r"/api/*": {"origins": "*"}})
4.3. Definition of Done (DoD)
一个功能特性的判定标准：

[ ] 实现了PRD中描述的所有功能。
[ ] 编写了并通过了所有相关的pytest测试。
[ ] 代码遵循项目编码规范。

[ ] 已成功合并到develop分支。